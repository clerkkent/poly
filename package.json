{
  "name": "polymarket-quant-system",
  "version": "1.0.0",
  "description": "Polymarket 多账户量化交易系统",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @poly/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @poly/web start",
    "api": "pnpm --filter @poly/api dev",
    "dev:packages": "pnpm --filter \"./packages/*\" watch",
    "watch": "pnpm --filter \"./packages/*\" watch",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

